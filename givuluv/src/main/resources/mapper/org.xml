<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gl.givuluv.mapper.OrgMapper">
	<insert id="insertOrg">
		insert into org(orgid, orgpw, orgname, logo, orgphone, ceoname, orgzipcode, orgaddr, orgaddrdetail, orgaddretc) 
		values(#{orgid},#{orgpw},#{orgname},#{logo},#{orgphone},#{ceoname},#{orgzipcode},#{orgaddr},#{orgaddrdetail},#{orgaddretc})
	</insert>
	<select id="getOrgById">
		select * from org where orgid=#{orgid}
	</select>
	<update id="updateUser">
		
	</update>
	<delete id="deleteUser">
		delete from org where orgid=#{orgid}
	</delete>
</mapper>